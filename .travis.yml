dist: xenial # required for Python >= 3.7
language: python
python:
  - "3.7"
install:
  - pip install -e .[dev]
script:
  - pytest
  - black --check mechwolf
  - flake8
  - mypy mechwolf/ --ignore-missing-import
cache: pip
