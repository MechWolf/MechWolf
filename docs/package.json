{
  "name": "mechwolf-docs",
  "version": "0.0.2",
  "description": "The documentation site for the MechWolf package",
  "main": "index.js",
  "scripts": {
    "dev": "npm run generate && vuepress dev ./",
    "build": "npm run install && npm run generate && vuepress build ./",
    "install": "python3.7 -V && python3.7 -m pip install -e .. ",
    "generate": "python3.7 generate_api_docs.py"
  },
  "author": "Benjamin Lee",
  "license": "GPL-3.0",
  "dependencies": {
    "@vuepress/plugin-medium-zoom": "^1.1.0",
    "vuepress": "^1.0.2"
  }
}
